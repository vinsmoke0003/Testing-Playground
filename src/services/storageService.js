// TODO: Integrate Firebase Storage for image uploads

export const uploadImage = async (imageUri) => {
  console.log('TODO: uploadImage to Firebase Storage. URI:', imageUri);
  // Return a mock URL for now
  return 'https://via.placeholder.com/300?text=Mock+Image+Upload';
};
